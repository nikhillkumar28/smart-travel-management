# ==========================================
# FRONTEND ENVIRONMENT VARIABLES (Vercel)
# ==========================================

# Base URL of the deployed Express backend (without trailing slash).
# Leave empty for local development (uses Vite dev proxy to localhost:3000).
# Example for production: https://your-travel-backend.onrender.com
VITE_API_BASE_URL=
